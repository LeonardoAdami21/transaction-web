import React from "react";

const Header: React.FC = () => {
  return (
    <div className="text-center mt-12 text-gray-500 text-sm">
      <p>Sistema de Transações - Frontend React + TypeScript</p>
    </div>
  );
};

export default Header;
